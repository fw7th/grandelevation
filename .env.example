# Copy this file to .env and fill in real values. .env is gitignored.

# Postgres connection string from Neon (or local Postgres for early dev)
DATABASE_URL=postgresql://user:password@host/dbname

# Will be added when we build auth (section D)
# SECRET_KEY=

# Will be added when we build payments (section D)
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
